#include "Mathematics.h"

double q2m(double* _q) { //I don't know what it should do, so I mock it.
	return 1;
}

double skew(double _w_e[3])
{
	matrix A;
	A.length = 3;
	A.width = 3;
http://www.geeksforgeeks.org/dynamically-allocate-2d-array-c/
	return _w_e[0];
}

double vectorNorm(double _r)  //Mocked it - Pablo has wrtitten the function
{
	return 1;
}


